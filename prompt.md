create the following automation: Given a target audio file in the 00 unprocessed audio recording folder. Use WhisperKit Large V3 Turbo. Extract the speakers, what each speaker says, and time stamps into a transcript as a markdown file.Save the markdown file in 01-transcripts folder. Download any needed dependencies. Either language models or audio models should be downloaded so that it can be used without having to re-download the model.
